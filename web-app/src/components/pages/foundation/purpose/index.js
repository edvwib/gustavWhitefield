import React from 'react';

import Page from './page';

const Purpose = (props) => {

  return (
    <Page eng={props.content.eng} page={props.content.pages.purpose}/>
  );

}

export default Purpose;
